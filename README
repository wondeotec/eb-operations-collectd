This project includes a python script to check the number of messages in a rabbitmq queue and outputs the value in collectd readable format.

Invocation of the script should be under the exec block on the collectd config file.